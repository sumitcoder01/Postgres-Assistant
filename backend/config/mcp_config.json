{
  "mcpServers": {
      "postgres": {
        "command": "docker",
        "args": [
          "run",
          "-i",
          "--rm",
          "--network", 
          "postgres-assistant_pg-assistant-net",
          "mcp/postgres",
          "${input:pg_url}"
        ],
        "transport": "stdio"
      }
    }
  }

